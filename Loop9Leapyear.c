#include<stdio.h>
main()
{
   int a,n;
   printf("Enter the value of n=");
   scanf("%d",&n);
   while (a<=n)
 
    {
      if(a%4==0)
      printf("Leap year is=%d\n",a);
	  a++;
    }
	
}
